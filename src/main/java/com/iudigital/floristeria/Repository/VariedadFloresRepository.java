package com.iudigital.floristeria.Repository;

import com.iudigital.floristeria.models.VariedadFlores;

import org.springframework.data.jpa.repository.JpaRepository;

public interface VariedadFloresRepository extends JpaRepository<VariedadFlores,Long> {
    
}
